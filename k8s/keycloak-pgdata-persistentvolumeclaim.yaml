apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    name: keycloak-pgdata
  name: keycloak-pgdata
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi